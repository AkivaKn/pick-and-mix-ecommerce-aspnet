@model IEnumerable<Sweet>

@{
    ViewData["Title"] = "Sweets";
}
<div class="row">
    @foreach(var item in Model)
    {
        <div class="col-md-4 col-xs-6 border-primary mb-3">
            <div class="card" style="max-width: 540px;">
                <img class="card-img-top" src="@item.ImageURL" alt="Item">
                <div class="card-body">
                    <p class="card-text">@item.Brand.BrandName</p>
                    <h5 class="card-title">@item.SweetName</h5>
                    <a class="btn btn-success"><i class="bi bi-cart-plus"></i> @((item.PriceInPence / 100d).ToString("c"))</a>
                </div>
            </div>
        </div>
    }
</div>